export { deepMerge } from "./deepMerge";
export { useArrayHelpers } from "./arrayHelpers";
// Re-export createDefaultData and other utilities from parent utils.ts
export {
  createDefaultData,
  normalizePath,
  getValueByPath,
  updateValueByPath,
} from "../utils";
